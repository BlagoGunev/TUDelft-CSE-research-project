#include <iostream>

#include <map>

#include <set>

#include <list>

#include <vector>

#include <queue>

#include <deque>

#include <cmath>

#include <ctime>

#include <cctype>

#include <bitset>

#include <iomanip>

#include <stack>

#include <string>

#include <cstring>

#include <numeric>

#include <functional>

#include <cassert>

#include <unordered_set>

#include <unordered_map>

#include <algorithm>

// #define int ll

#define L(u) u<<1

#define R(u) u<<1|1

#define lowbit(u) u&(-u)

#define pcount(x) x&(x-1)

#define InRange(l,r,ll,rr) (ll<=l)&&(rr>=r)

#define fori(i,n) for(int i=1;i<=n;i++)

#define rep(i,n) for(int i=n;i;i--)

#define pii pair<int,int>

#define pb push_back

#define mpi make_pair

using namespace std;

using ll = long long;

int t;

void solve()

{

    int k;

    cin>>k;

    cout<<k-1<<"\n";

}

int main()

{

    cin.tie(nullptr)->sync_with_stdio(false);

    cin>>t;

    while(t--)

        solve();

    return 0;

}